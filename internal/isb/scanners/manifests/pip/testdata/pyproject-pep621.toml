[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "myproject"
version = "0.1.0"
dependencies = [
    "requests==2.31.0",
    "click>=8.0",
    "rich==13.7.0"
]

[project.optional-dependencies]
test = ["pytest==7.4.0"]
